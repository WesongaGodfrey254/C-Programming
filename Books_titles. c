/*
Name:Godfrey Wesonga
RegNo:PA106/G/28754/25
Description: Codes that allows librarian to enter books
             titles and store each title in a text file.
*/

#include <stdio.h>

int main() {
    FILE *fp;
    char title[100];

    fp = fopen("borrowed_books.txt", "a"); // open in append mode
    if (fp == NULL) {
        printf("Error opening file!\n");
        return 1;
    }

    printf("Enter book title: ");
    fgets(title, sizeof(title), stdin); // read title with spaces

    fprintf(fp, "%s", title); // write to file
    fclose(fp);

    printf("Book title successfully stored in borrowed_books.txt\n");
    return 0;
}
